/*
	Ejercicio 3
	
	Escriba un programa que solicite al usuario ingresar un número y luego determine si el mismo es par o impar. Esta vez no utilice la función MOD(),hágalo por el método de división por restas sucesivas.
*/

DECLARE
	V_DIVISOR		NUMBER;
	V_DIVIDENDO		NUMBER;
	V_RESTO			NUMBER;
	V_COCIENTE		NUMBER;
BEGIN 
	
	V_DIVISOR := &a;

	FOR indice IN V_DIVISOR LOOP

	END LOOP;
	
END;
